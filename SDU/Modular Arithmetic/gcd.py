# This script finds gcd (Greatest common divisor) for a given n and m (where n and m are non-negative integers).

import math

print("gcd(n, m)")
n = int(input("n = "))
m = int(input("m = "))
print(math.gcd(n, m))
